name = input("What's your name? ")
print(f"Hi {name}, welcome to Python!")

'''
📌 Concepts Covered:

String input and storage

f-string formatting (Python 3.6+)

🧠 Logic:

Reads a string via input(), stores in variable name.

Uses f-string to dynamically insert the name into the output message.'''